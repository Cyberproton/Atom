package project.cyberproton.atom.condition.loader;

import org.jetbrains.annotations.NotNull;
import org.jetbrains.annotations.Nullable;
import org.spongepowered.configurate.ConfigurationNode;
import project.cyberproton.atom.condition.Condition;
import project.cyberproton.atom.util.Loader;

public class ConditionLoader implements Loader<Condition> {
    @Override
    public @Nullable Condition load(@NotNull ConfigurationNode node) {
        return null;
    }
}
